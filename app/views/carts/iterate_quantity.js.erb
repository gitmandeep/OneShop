<% if @cart_object_removed %>
  location.reload();
<% else %>
  $('.product-quantity-<%= @cart_product.product.id %>').html("<%= @cart_product.quantity %>")
  $('.product-price-<%= @cart_product.product.id %>').html("₹ <%= @cart_product.product.price.to_i*@cart_product.quantity.to_i %>")
<% end %>


alert('Only ' + '<%= @remaining %>' + ' products left.')